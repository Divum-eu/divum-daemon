info:
  name: DeleteMinecraftServer
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{address}}/{{base_route}}/minecraft-servers/{{server_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
