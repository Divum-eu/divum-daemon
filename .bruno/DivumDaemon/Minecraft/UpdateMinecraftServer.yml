info:
  name: UpdateMinecraftServer
  type: http
  seq: 4

http:
  method: POST
  url: "{{address}}/{{base_route}}/minecraft-servers/2bf0025e-b3e2-4a10-993a-417a612a209d/update"
  body:
    type: json
    data: |-
      {
        "server_address": "127.0.0.2",
        "memory_limit": 4096,
        "cpu_cores_limit": 1,
        "eula": true,
        "version": "LATEST",
        "type": "VANILLA",
        "rcon_password": "parola",
        "server_name": "Divum's world",
        "online_mode": false,
        "mode": "creative"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
